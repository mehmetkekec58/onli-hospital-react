import LoginModel from "../models/loginModel";
import RegisterModel from "../models/registerModel";

export function login(login:LoginModel) {
    return true;
}
export function register(register:RegisterModel) {
    return true;
}